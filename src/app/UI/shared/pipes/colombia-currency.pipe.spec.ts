import { ColombiaCurrencyPipe } from './colombia-currency.pipe';

describe('ColombiaCurrencyPipe', () => {
  it('create an instance', () => {
    // const pipe = new ColombiaCurrencyPipe();
    // expect(pipe).toBeTruthy();
  });
});
